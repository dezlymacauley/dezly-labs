/*
    ABOUT: Linked List 
*/

struct Node {

}

// Linked List struct

// Linked List constructor that accepts `value which is an int`
// creates a list
// creates a new node and makes it the first node of the list.

// Append function (returns nothing, accepts value which is an int)
// creates a new node and adds to the end of the list.

// Prepend function (returns nothing, accepts value which is an int)
// creates a new node and adds to the beginning of the list.

// insert function (accepts and index which is an int, 
// and a value which is an int)
// creates a new node and inserts that node at the index specified.
// returns a bool.

fn main() {}
