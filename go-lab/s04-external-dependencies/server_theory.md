web server = serve web pages for a website

Response request model

Client makes a request for some data and the server responds to the request.

Client makes a request using a url

The web server generates the response to the request.
It server html, css, and javascript files.
Also serves images and videos.

Handles http error messages. status codes

Handles users requests in a concurrent way.

Directs url matching and rewriting.

Processes and serves dynamic content.

_______________________________________________________________________________

api (application programmin interface)

A set of definitions and protocols for building and integrating application
software. Allow programs to communicate with an application.

_______________________________________________________________________________

Both can be connected to a database for persistent storage.
_______________________________________________________________________________
