The HTTP protocol defines a set of request methods for indicating the
action you want to peform for a specific resource.

GET = Used for retrieving data from an API

POST = submits data to the API

PUT = updates the resource
